var EditorTable = (function() {

    var init_table = function(editor, row, col) {
        editor.table($table);
        return $table;
    };

    var editor_table = function(editor, row, col) {
        var me = this;
        me.editor = editor;
        me.col = col;
        me.row = row;
        me.$table = editor. row, col);
    };

    editor_table.prototype.current = function() {
    };

    editor_table.prototype.delCol = function(col, count) {
        if (col >= this)
    };

    editor_table.prototype.delRow = function(row, count) {
    };
})();
